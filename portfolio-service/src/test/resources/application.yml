spring:
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: portfolio-service-test
      auto-offset-reset: earliest
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

gateway:
  secret: test-secret
