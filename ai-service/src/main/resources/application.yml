spring:
  application:
    name: ai-service
  config:
    import: optional:configserver:http://config-server:8888

logging:
  structured:
    format:
      console: logstash
      file: logstash






