# 06. AI 설계 검증 로그

> 이 로그는 면접/포트폴리오용 핵심 산출물입니다. "AI가 해줬다"가 아니라
> "AI를 활용해 이런 판단을 내렸다"를 보여주는 문서입니다.
> Claude는 새로운 설계 의사결정이 내려질 때마다 이 문서에 행을 추가하도록 돕고,
> **이미 기록된 결정(특히 "거부" 항목)을 다시 권유하지 않습니다.**

---

## 기존 의사결정 (확정됨 — 재논의 시 강한 근거 필요)

### DDD / 서비스 경계

| 검증 항목       | AI 프롬프트 (요약)                        | AI 피드백 (요약)                                                                                                                                                                           | 수용 여부 | 판단 근거                                              |
|-------------|-------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------|----------------------------------------------------|
| 사용자 + 인증 분리 | 사용자 기능과 인증 기능을 별도 컨텍스트로 분리하는 게 나을까? | Auth Context(JWT 발급, 로그인/로그아웃, 토큰 갱신 — 기술적 관심사)와 User Context(프로필, 성향, 관심사, 홈 피드 — 비즈니스 관심사)는 변경 이유가 다르므로 분리 추천. 다만 팀 규모가 작으면 오버엔지니어링일 수 있어, 처음엔 하나로 시작하고 Auth 로직이 복잡해질 때 분리하는 것도 현실적 | ❌ 거부  | 일단 하나의 서비스(user-service)로 개발하고, 이후 필요 시 별도 서비스로 분리 |

> **→ Claude에게**: user-service 안에 인증(Auth)과 사용자(User) 도메인을 함께 두는 것이
> 현재 합의된 구조입니다. "Auth Context를 분리하자"는 제안은 다시 하지 마세요. 다만 패키지
> 내부적으로 `auth` / `user` 서브패키지로 논리적 분리는 가능합니다 (이건 새로운 제안으로
> 제시해도 됨).

### 시세 조회 아키텍처 (검토 중 — pending)

| 검증 항목 | AI 프롬프트 (요약)                                    | AI 피드백 (요약)                                                                                                                                                                                      | 수용 여부       | 판단 근거   |
|-------|-------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|---------|
| 주가 조회 | 실시간 주가 지표 조회 기능에서 많은 종목 정보를 지연 없이 빠르게 조회/저장하려면? | 종목 1만 개 × 10개 필드를 Redis Hash로 저장해도 약 50~100MB로 충분히 감당 가능, Redis Cluster로 샤딩 시 수십만 종목도 가능. raw tick은 Kafka 버퍼 → 집계 컨슈머가 1분 단위 OHLCV 생성 → TimescaleDB 적재, raw tick 원본은 S3 Parquet → 필요 시 Athena 분석 | **?** (미결정) | (작성 필요) |

> **→ Claude에게**: 이 항목은 아직 수용/거부가 결정되지 않았습니다. stock-service의 시세
> 저장/조회 구조를 구현하기 전에, 이 Redis+Kafka+TimescaleDB+S3 구조를 그대로 적용할지,
> 더 단순한 구조(예: Redis만 + 주기적 DB 스냅샷)로 갈지 사용자에게 먼저 물어보고, 결정되면
> 이 행을 갱신하세요. (TimescaleDB/S3/Athena는 현재 기술 스택 목록(`04-INFRA-ARCHITECTURE.md`)에
> 없으므로 추가 인프라가 필요하다는 점도 함께 안내하세요.)

### AI 서비스 — 뉴스 수집 방식

| 검증 항목               | AI 프롬프트 (요약)                                          | AI 피드백 (요약)                                                                          | 수용 여부 | 판단 근거                         |
|---------------------|-------------------------------------------------------|--------------------------------------------------------------------------------------|-------|-------------------------------|
| 뉴스 크롤링 vs API fetch | 뉴스를 가져올 때 크롤링이 좋을지, 뉴스 API fetch가 좋을지, 어떤 뉴스 API가 있는지 | 모의 투자 서비스 맥락에서 크롤링은 리스크 대비 이득이 없음. 구현 난이도, 저작권 문제, 안정성/성능 모두 API가 우위이므로 뉴스 API 사용 권장 | ✅ 수용  | (별도 기재 없음 — "뉴스 API 사용"으로 결정) |

> **→ Claude에게**: ai-service의 뉴스 수집(`POST /api/v1/ai/news/fetch`)은 **뉴스 API 호출**
> 방식으로 구현합니다. 크롤링 코드를 제안하지 마세요. 구체적인 뉴스 API 제공자는 아직
> 명시되지 않았으므로, 구현 시작 시 사용자에게 어떤 API(예: 네이버 뉴스 API, 금융 특화
> 뉴스 API 등)를 쓸지 확인하세요.

### 인프라 — 오케스트레이션

| 검증 항목                        | AI 프롬프트 (요약)                                                                                               | AI 피드백 (요약)                                                                                                                                                                                                                       | 수용 여부 | 판단 근거                                                          |
|------------------------------|------------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------|----------------------------------------------------------------|
| Docker Compose vs Kubernetes | MVP 단계에서 Spring Cloud 기반 vs Kubernetes 네이티브 아키텍처 중 어떤 방향이 적절한지, EC2 소규모 환경에서 복잡도/운영 난이도/확장성 측면에서 합리적인 선택은? | 이미 K8s 설계가 완료된 EC2 소규모 환경이라면 K8s Native를 유지하고 Spring Cloud 인프라 컴포넌트(Eureka, Config Server)는 제거하는 것이 맞음. K8s가 서비스 디스커버리(DNS), 설정 관리(ConfigMap/Secret), 라우팅(Ingress)을 이미 제공하므로 별도 JVM 추가 불필요. Resilience4j처럼 라이브러리로 쓸 수 있는 것만 남기면 됨 | ❌ 거부  | 현재 MVP에서는 k8s를 사용하지 않기로 함 — k8s 사용으로 인한 유지보수/학습 비용 및 추가 금액적 부담 |

> **→ Claude에게**: 이 프로젝트는 **Eureka + Config Server를 유지**하기로 결정했습니다
> (AI 피드백과는 반대 방향). `k8s/` 디렉토리가 저장소에 있지만 "k8s로 전환하자"는 제안은
> 다시 하지 마세요. docker-compose 기반 운영이 기본입니다.

### admin-service — 관리자 인증 방식

| 검증 항목                              | AI 프롬프트 (요약)                                                                                                                     | AI 피드백 (요약)                                                                                                                                                                                       | 수용 여부 | 판단 근거                                                                                                               |
|------------------------------------|----------------------------------------------------------------------------------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------|---------------------------------------------------------------------------------------------------------------------|
| admin-service 인증 방식으로 Okta OIDC 채택 | Okta는 타 OTP와 달리 기기 변경·변동 시 수동 등록/초대가 필요해 관리자 접근을 엄격히 통제할 수 있다. 타 서비스는 계정/클라우드 기반이라 보안상 위험하다고 판단해 어드민 페이지에 Okta를 채택했는데, 어떻게 생각하나? | 동의. Okta의 초대 기반 접근 모델은 소수 관리자 환경에서 무단 접근 차단에 유리하고, Spring Security OAuth2 Client와 통합이 간결함. 계정/클라우드 기반 OTP 대비 기기 등록·해제를 IdP 단에서 직접 통제해 침해 시 대응이 빠름. 트레이드오프: Okta 무료 티어 한도(MAU 100명), 외부 서비스 의존성 존재 | ✅ 수용  | 관리자는 소수·고신뢰 계정이므로 무료 한도가 문제되지 않음. 인증 앱 계정 탈취 시 관리자 권한이 넘어갈 수 있는 클라우드 기반 OTP보다, 초대 기반의 명시적 접근 통제가 이 서비스 규모에 적합하다고 판단 |

> **→ Claude에게**: admin-service는 Okta OIDC(OAuth2 Login)로 인증합니다.
> Form Login 방식 또는 user-service에 Feign으로 인증을 위임하는 방식으로의 변경을 재제안하지 마세요.

---

### 소셜 로그인 인증 흐름

| 검증 항목                  | AI 프롬프트 (요약)                                                                                      | AI 피드백 (요약)                                                                                                                  | 수용 여부 | 판단 근거                                                                  |
|------------------------|---------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------|-------|------------------------------------------------------------------------|
| 소셜 로그인 방식 선택           | Google/Kakao 소셜 로그인 시 ID Token 검증 / Authorization Code+PKCE / access_token 전달 중 2026년 기준 권장 방식 검토 | OAuth 2.1, RFC 9700 BCP 기준 confidential client(백엔드)에도 PKCE 사용을 권장. PKCE로 인가 코드 탈취·주입을 방어하고, client_secret으로 클라이언트 신원을 별도로 검증 | ✅ 수용  | 두 방어 수단을 함께 적용하는 게 보안 표준에 부합한다고 판단, redirect_uri 관리 비용은 감당 가능한 수준으로 평가 |
| Google/Kakao 흐름 통일     | 기존 Google(ID Token 검증) / Kakao(access_token 전달) 간 비일관성 해소 방안 검토                                   | 두 provider 모두 Authorization Code+PKCE로 통일 가능, `OAuthClient` 인터페이스로 추상화 제안                                                    | ✅ 수용  | provider 추가·교체 시 동일 인터페이스로 확장 가능해 유지보수성 개선                             |
| CSRF(state) 파라미터 누락    | 코드 리뷰 중 PKCE만으로 CSRF까지 방어되는지 재검토                                                                  | RFC 9700상 PKCE는 인가 코드 탈취·주입 방어용이며, CSRF는 state 파라미터로 별도 방어해야 한다는 피드백                                                         | ✅ 수용  | 생성·검증 주체를 프론트로 명확히 하고 로그인 URL에 state 파라미터 추가                           |
| 카카오 이메일 동의 처리          | 카카오 사용자가 이메일 제공에 동의하지 않은 경우 처리 방식 검토                                                              | `email_needs_agreement`, `is_email_valid` 등 필드를 추가 확인하지 않으면 누락된 이메일을 그대로 신뢰하는 버그 가능성 지적                                      | ✅ 수용  | 3개 필드 동시 검증 로직 추가, 동의 안 한 경우 명시적으로 예외 처리                               |
| 이메일 열거(enumeration) 방지 | 동일 이메일로 일반가입 후 소셜 로그인 시도 시 409 응답이 이메일 존재 여부를 노출하는 문제 검토                                          | 응답 코드·메시지로 가입 여부가 추론 가능해 보안상 모호하게 처리할 것을 제안                                                                                  | ✅ 수용  | 400 + 모호한 메시지로 변경, 상태 코드만으로 이메일 존재 여부를 알 수 없도록 수정                      |

> **현재 상태**: 백엔드 구현 완료 (`feat/user-oauth-social-login`), 프론트엔드는 아직 미착수.
> - 흐름: 프론트 → code_verifier/code_challenge/state 생성 → `POST /api/v1/auth/social/login-url` → 리다이렉트 → 콜백 code 수신 → `POST /api/v1/auth/social/login` → JWT 발급
> - redirect_uri는 프론트엔드 콜백 URL로 설정 (백엔드에 별도 콜백 엔드포인트 없음)
> - 카카오 개발자 콘솔에서 인가 코드 요청 시 PKCE 옵션 활성화 필요
>
> **→ Claude에게**: 위 5개 항목은 이미 검증·구현이 끝난 결정입니다. 같은 내용으로 다시 검토를 권유하지 마세요. 소셜 로그인 관련 작업을 이어갈 때는 프론트엔드가 아직 연동되지 않았다는 점을 고려해 주세요.
---

### stock-service(KIS) 장애 격리 — Circuit Breaker & Fallback 정책

| 검증 항목 | AI 프롬프트 (요약) | AI 피드백 (요약) | 수용 여부 | 판단 근거 |
| --- | --- | --- | --- | --- |
| KIS 점검/장애 시 장애 전파 문제 | KIS가 점검·장애일 때 stock-service에 의존하는 다른 서비스까지 줄줄이 장애가 나서 MSA 장점이 사라지는데 어떻게 막을지 | 조사 결과 실제 동기 결합은 trade-service→stock-service 하나뿐(portfolio-service는 yml 설정만 준비, ai/notification/payment는 미착수). trade-service는 Feign 호출은 있으나 Resilience4j/CircuitBreaker가 전혀 없고 try/catch 수동 매핑뿐. stock-service 내부의 `@CircuitBreaker(redisPrice)`는 KIS 호출이 아니라 자체 Redis 캐시 접근만 보호 중 | ✅ 수용 | trade-service 쪽에 Resilience4j CircuitBreaker+Fallback을 우선 추가하기로 함(가장 시급한 실제 결합) |
| 모든 호출자에 동일한 "기본값/캐시값 반환" fallback 적용 | 장애 시 모든 의존 서비스에 같은 방식(예: 마지막 캐시값 반환)으로 fallback하면 되는지 | 서비스 성격에 따라 위험도가 다름 — trade-service(매수/매도)는 stale 가격으로 체결되면 오히려 더 큰 금전적 사고로 이어짐. 반면 portfolio-service(대시보드 조회)는 약간 오래된 시세를 보여줘도 사용자 피해가 적음. 획일적 fallback 정책은 위험 | ❌ 거부 | trade-service는 **fail-fast**(시세 조회 실패 시 주문 자체를 명확히 거부 응답)로, portfolio-service류 조회 전용 서비스는 **graceful degrade**(stale 캐시 + "지연됨" 표시)로 서비스별 차등 정책을 적용하기로 함 |
| portfolio-service 등 향후 stock-service 결합 방식 (동기 Feign+CB vs 이벤트 기반) | 앞으로 stock-service에 결합할 서비스(portfolio-service 등)는 Feign+CircuitBreaker로 갈지, Kafka 이벤트 구독으로 갈지 | stock-service가 이미 KIS WebSocket으로 실시간 시세를 받고 있으므로, 실시간성이 필수가 아닌 조회 성격 서비스는 `stock.price.updated` 류 Kafka 이벤트를 구독해 자체 캐시를 유지하는 방식이 MSA 가이드라인의 "동기 호출 최소화" 원칙과 부합하고, KIS/stock-service 장애가 발생해도 마지막 수신 이벤트로 계속 서비스 가능해 장애 전파를 원천 차단함 | ✅ 수용 | 트레이드오프(결과적 일관성, Kafka 컨슈머 구현 비용)를 감안해도, 조회 전용 서비스의 가용성을 지키는 이득이 더 크다고 판단. trade-service처럼 그 순간의 정확한 가격이 필수인 경로는 여전히 동기 호출+CB 유지 |

> **현재 상태**: stock-service(영욱 담당) 쪽 1·2순위 구현 완료, fail-fast로 확정. stale-cache fallback은 보류(다음 항목 참고). **담당 범위 상 trade-service는 동민, stock-service는 영욱 담당**이므로, 구현은 stock-service만 진행했고 trade-service 변경은 동민에게 요청만 한다.
> - 코드 확인 결과, trade-service는 이미 부분적 방어가 있음: `TradeService.getCurrentPrice()`, `AssetService.validateStockData()` 둘 다 `price <= 0`이면 예외를 던져 잘못된 가격 체결은 이미 막고 있음. 다만 `TradeService.buyStock/sellStock`에는 `FeignException`/`RetryableException` catch가 없어(AssetService에는 있음) stock-service가 완전히 죽으면 500으로 샐 수 있음 — 이건 동민이 고칠 부분, Claude가 임의로 trade-service 코드를 수정하지 말 것.
> - ✅ 완료(stock-service, 영욱): `StockService.resolvePrice()`/`fetchAndCacheCurrentPrice()`가 Redis/KIS 실패 시 `price=BigDecimal.ZERO`인 정상(200) 응답으로 뭉개던 부분 제거. Redis만 죽었으면 KIS로 직접 재조회하고, KIS까지 실패하면 `CustomException(StockErrorCode.KIS_CONNECTION_FAILED)`(503)를 그대로 던짐.
> - ✅ 완료(stock-service, 영욱): `KisOAuthClient`의 `getCurrentPrice`/`getCandle`/`getVolumeRank`/`getThemeInfo`에 `@Retry(name="kisApi")` + `@CircuitBreaker(name="kisApi")` 추가, `config-server/stock-service.yml`에 `resilience4j.retry.instances.kisApi`/`resilience4j.circuitbreaker.instances.kisApi` 설정 추가. 실패 시 `kisApiFallback`이 동일하게 503을 던짐. 부수적으로 `StockPriceRedisAdapter.savePrice`가 Redis 쓰기 실패(`DataAccessException`)를 로그만 남기고 무시하도록 수정(이미 KIS에서 받아온 가격까지 날아가지 않게).
> - **확정**: stale 값을 서빙하는 graceful degrade가 아니라 순수 fail-fast — 캐시 미스 + KIS 실패 시 예전 값/기본값 없이 503만 반환. `isStale`/`asOf` 메타데이터를 붙여 마지막 성공 값을 서빙하는 stale-cache fallback은 **나중으로 보류**. 필요해지면(예: portfolio-service가 조회 전용으로 stock-service를 직접 호출하게 될 때) 재논의.
> - trade-service 쪽에 전달할 사항(코드 작성 요청 아님): "stock-service가 장애 시 price=0 대신 503을 던지도록 바뀌었으니, `TradeService.buyStock/sellStock`에도 `AssetService.requestStockData`처럼 FeignException/RetryableException 캐치를 추가해달라."
> - portfolio-service 등 신규 결합은 Feign+CB가 아니라 Kafka 이벤트 구독(`stock.price.updated`)으로 구현 — 새로 Feign 동기 호출을 추가하자는 제안은 하지 말 것.
> - 시세 저장/조회 자체의 인프라 구조(Redis+Kafka+TimescaleDB 등)는 위 "시세 조회 아키텍처(pending)" 항목과 별개로, 아직 미결정 상태이니 혼동하지 말 것.
>
> **→ Claude에게**: 1·2순위는 구현 완료된 상태입니다. stale-cache fallback을 다시 제안하지 말고, 사용자가 먼저 꺼내기 전까지는 이 상태를 유지하세요. trade-service 코드는 절대 직접 수정하지 말고, 필요한 변경사항은 위처럼 "동민에게 전달할 메시지" 형태로만 안내하세요.

---

## 새 검증 항목 작성 템플릿

새로운 설계 결정을 검증할 때는 아래 형식으로 행을 추가하세요. (필수 제출 기준: 검증 항목
3개 이상, 그중 최소 1개는 "거부"와 근거 포함)

```markdown
| 검증 항목 | AI 프롬프트 (요약) | AI 피드백 (요약) | 수용 여부 | 판단 근거 |
| --- | --- | --- | --- | --- |
| (예: Kafka 파티션 키 선택) | (사용자가 던진 질문 요약) | (Claude 피드백 요약) | ✅ 수용 / ❌ 거부 | (왜 수용/거부했는지, 우리 서비스 규모·맥락 기준으로) |
```

작성 시 유의사항:
- AI 피드백을 그대로 복붙/수용하지 말고, **우리 프로젝트 규모·일정·맥락에 맞는지**를
  기준으로 판단할 것.
- "거부" 항목은 반드시 근거를 적을 것 (예: "이론적으로는 맞지만 프로젝트 규모상 과도한
  엔지니어링", "트래픽 패턴상 동시성 이슈가 크지 않음" 등).
- Claude는 검증을 요청받으면 위 `05-MSA-GUIDELINES.md` 6번의 "좋은 프롬프트" 형태로
  질문을 구체화하도록 도와주고, 트레이드오프를 반드시 함께 제시합니다.
